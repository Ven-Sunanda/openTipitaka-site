This folder contains static assets for the GitHub Pages landing page.

- `icon-192.png`, `icon-512.png`: copied from the app icon source for branding.
- `screenshots/*.svg`: illustrative UI previews for the **Screenshots** section on the landing page. Replace with real PNG/WebP captures (same basenames or update paths in `index.html`) when ready.

